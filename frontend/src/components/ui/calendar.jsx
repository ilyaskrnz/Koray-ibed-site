import * as React from "react"

// Calendar component placeholder - not used in this project
function Calendar({ className, ...props }) {
  return null;
}

Calendar.displayName = "Calendar"

export { Calendar }
